ci:
  collect:
    url:
      ['http://localhost/', 'http://localhost/exam', 'http://localhost/results']
    staticDistDir: './dist'
    isSinglePageApplication: true
  upload:
    target: 'temporary-public-storage'
  assert:
    presets: lighthouse:all
